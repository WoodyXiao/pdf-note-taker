export default {
  // Local PostCSS config for the React frontend.
  // This intentionally overrides the root Next.js postcss.config.mjs
  // to avoid loading Next-specific plugins in the Vite app.
  plugins: [],
};


